/*fetch the data from a SQLite database*/

SELECT first_name from Person;
SELECT first_name, age from Person;
SELECT distinct color from EyesColor;
SELECT first_name, last_name, age from Person
       ORDER BY age ASC;
